import React from "react";
import Recharts from "recharts";

function Chart() {
  const {
    LineChart,
    Line,
    XAxis,
    YAxis,
    CartesianGrid,
    Tooltip,
    Legend
  } = Recharts;

  <div></div>;
}

export default Chart;
